apiVersion: v1
kind: Service
metadata:
  name: {{ include "fas.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fas.selectorLabels" . | nindent 4 }}
