apiVersion: v1
kind: Service
metadata:
  name: {{ include "browser.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "browser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "browser.selectorLabels" . | nindent 4 }}
