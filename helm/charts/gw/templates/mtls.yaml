apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-a
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-a
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-hm
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-hm
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-hmc
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-hmc
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-omni
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-omni
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-oc
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-oc
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-bw
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-bw
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-bw-api
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-bw-api
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-search
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-search
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: envoy-mtls-toggle
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy
      sectionName: https-mtls-toggle
  tls:
    alpnProtocols:
      - http/1.1
      - h2
    clientValidation:
      caCertificateRefs:
        - name: mtls-ca-bundle
---
apiVersion: v1
kind: Secret
metadata:
  name: mtls-ca-bundle
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  tls.ca: {{ printf "%s\n%s" (.Files.Get "certs/mtlsca.crt") (.Files.Get "certs/mtlsca-095.crt") | b64enc | quote }}
